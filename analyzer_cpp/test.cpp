#include <iostream>

int main() {
    int x = 5;      // This variable is declared but never used.
    int y = 10;
    int z;          // This variable is declared but never used.

    std::cout << "The value of y is: " << y << std::endl;

    return 0;
}